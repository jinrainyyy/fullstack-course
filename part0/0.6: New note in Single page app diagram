sequenceDiagram
    participant browser
    participant server

    Note left of server: The browser creates a new note, adds it to the notes list and rerenders the note list on the page and sends the new note to the server.

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: the result of submission
    deactivate server

    
